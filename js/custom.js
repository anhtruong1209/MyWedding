document.getElementById("menu-btn").addEventListener("click", function() {
    var nav = document.querySelector("nav");
    nav.classList.toggle("open"); // Toggles the open class to show/hide the menu
});